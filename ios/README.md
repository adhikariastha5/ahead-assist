# AI Walker iOS app

The SwiftUI AI Walker prototype lives in `ios/AIWalker.xcodeproj`.

Open the project in Xcode, select the **AIWalker** scheme and **My Mac**, then press `Command + R`.

The included Core ML YOLO model is used for the video safety simulation. The Melbourne CBD navigation flow is a demo with curated routes and must not be used as live accessibility guidance.
